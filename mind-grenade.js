const num1 = 5
const num2 = 10
// Функция - это блок кода, который можно вызывать.
// Функция - это способ организовать код, чтобы его можно было переиспользовать.
// Функция только задает, что делать, но не выполняет это действие.
// То есть addValues - это функция, которая выполняет определенные действия
function addValues() {
    console.log(`the sum is : ${num1 + num2}`);
}   

addValues() // То есть здесь мы вызываем функцию addValues, которая была определена выше